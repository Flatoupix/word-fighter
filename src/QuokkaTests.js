let wordArrayAdjacent = ['abaca', 'boulanger', 'crenouille', 'drague', 'etirer', 'foutre', 'gagner']

function isAdjacentLetter(word) {
  if (
    this.letters.indexOf(word) === this.letters.indexOf(word) + 1 ||
    this.letters.indexOf(word) === this.letters.indexOf(word) - 1
  ) {
  }
}

isAdjacentLetter()
